<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FQS_Lifetime_Donor_Level_Name__c</fullName>
    <description>Text formula that returns the branded donor grouping name (e.g., Friend, Partner, Champion) based on TotalGiftsAmount against the Lifetime_Min_Amount__c thresholds in FQS_Donor_Grouping custom metadata. Returns blank when TotalGiftsAmount is below the Entry lifetime threshold. Thresholds and branded names are configured via Setup → Custom Metadata Types → FQS Donor Grouping or the FQS Setup screen flow. Changing threshold values or branded names recalculates this field across all Donor Gift Summary records retroactively — no redeployment or batch job required. Reports and dashboards will reflect new values immediately.</description>
    <externalId>false</externalId>
    <formula>IF(TotalGiftsAmount >= $CustomMetadata.FQS_Donor_Grouping__mdt.Major.Lifetime_Min_Amount__c, $CustomMetadata.FQS_Donor_Grouping__mdt.Major.Branded_Name__c,
IF(TotalGiftsAmount >= $CustomMetadata.FQS_Donor_Grouping__mdt.Mid.Lifetime_Min_Amount__c, $CustomMetadata.FQS_Donor_Grouping__mdt.Mid.Branded_Name__c,
IF(TotalGiftsAmount >= $CustomMetadata.FQS_Donor_Grouping__mdt.Entry.Lifetime_Min_Amount__c, $CustomMetadata.FQS_Donor_Grouping__mdt.Entry.Branded_Name__c,
"")))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Automatically calculated. Shows the branded donor grouping name for this donor based on their total lifetime giving (e.g., Friend, Partner, Champion). Blank means the donor is below the Entry threshold. Your administrator controls both the threshold amounts and donor grouping names.</inlineHelpText>
    <label>Lifetime Donor Grouping Name</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
