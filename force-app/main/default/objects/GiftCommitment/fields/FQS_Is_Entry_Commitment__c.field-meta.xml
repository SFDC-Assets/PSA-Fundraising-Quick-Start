<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FQS_Is_Entry_Commitment__c</fullName>
    <description>Boolean formula that returns TRUE when ExpectedTotalCmtAmount meets or exceeds the Entry.Lifetime_Min_Amount__c value in FQS_Donor_Grouping custom metadata. GiftCommitment has no CurrentAmount field, so ExpectedTotalCmtAmount (total pledged value) is used as the commitment's amount basis. Lifetime thresholds are used because a commitment represents a total pledge, not a single transaction. Thresholds are configured via Setup → Custom Metadata Types → FQS Donor Grouping or the FQS Donor Grouping Configurator screen flow. Changing threshold values recalculates this field across all Gift Commitment records retroactively — no redeployment or batch job required. Reports and dashboards using this field will reflect the new values immediately.</description>
    <externalId>false</externalId>
    <formula>ExpectedTotalCmtAmount >= $CustomMetadata.FQS_Donor_Grouping__mdt.Entry.Lifetime_Min_Amount__c</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Automatically calculated. Marked TRUE when this commitment's total expected amount qualifies for the Entry giving tier. Your administrator controls the threshold amount.</inlineHelpText>
    <label>Is Entry Commitment</label>
    <required>false</required>
    <type>Checkbox</type>
</CustomField>
