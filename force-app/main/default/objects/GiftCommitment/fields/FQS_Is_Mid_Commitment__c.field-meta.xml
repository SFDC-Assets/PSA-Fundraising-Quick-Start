<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FQS_Is_Mid_Commitment__c</fullName>
    <description>Boolean formula reading the FQS Donor Grouping CMDT thresholds. Configure via Setup → Custom Metadata Types or the FQS Setup flow → Configure Donor Groupings.</description>
    <externalId>false</externalId>
    <formula>AND(
  ExpectedTotalCmtAmount &gt;= $CustomMetadata.FQS_Donor_Tier__mdt.Mid.Lifetime_Min_Amount__c,
  ExpectedTotalCmtAmount &lt; $CustomMetadata.FQS_Donor_Tier__mdt.Major.Lifetime_Min_Amount__c
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Set automatically. Checked when this commitment's total expected amount falls in the corresponding donor grouping. Your administrator controls the threshold through the FQS Setup flow.</inlineHelpText>
    <label>Is Mid Commitment</label>
    <required>false</required>
    <type>Checkbox</type>
</CustomField>
