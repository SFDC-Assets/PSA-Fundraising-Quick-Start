<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="urn:metadata.tooling.soap.sforce.com">
    <fullName>ScheduleType</fullName>
    <description>Restricted picklist. Legal values: **Recurring** (platform fans out installments from `TransactionPeriod` + `TransactionInterval` + `TransactionDay`), **Custom** (irregular installments — each `GiftTransaction` must be inserted by hand). Platform-managed on Flow inserts (silently overrides to `Recurring` unless left blank). Apex can set `Custom` on insert explicitly.

This is a system field that describes the schedule attached to the commitment — not the type of gift itself. A wide variety of pledges and gifts can have a `Recurring` value here, not just open-ended online recurring donations for a set amount.</description>
    <inlineHelpText>Leave blank for standard recurring gifts and pledges — the platform will fill this in from the schedule you attach. Set to *Custom* only when the schedule has irregular installment amounts or spacing (typical for grants).</inlineHelpText>
</CustomField>
